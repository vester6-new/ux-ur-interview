export const metadata = {
  title: "UX Research Feedback",
  description: "UX interview feedback survey and dashboard",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
